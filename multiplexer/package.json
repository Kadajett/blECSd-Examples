{
	"name": "multiplexer-example",
	"version": "0.0.1",
	"description": "Terminal multiplexer demo - tmux-like multi-pane terminal with shell support",
	"type": "module",
	"main": "dist/index.js",
	"bin": {
		"multiplexer-demo": "dist/index.js"
	},
	"scripts": {
		"build": "tsup index.ts --format esm --dts",
		"start": "node dist/index.js",
		"dev": "tsx index.ts"
	},
	"dependencies": {
		"blecsd": "^0.1.0"
	},
	"devDependencies": {
		"@types/node": "^25.2.1",
		"tsup": "^8.5.1",
		"tsx": "^4.19.3",
		"typescript": "^5.8.2"
	},
	"optionalDependencies": {
		"node-pty": "^1.1.0"
	}
}
